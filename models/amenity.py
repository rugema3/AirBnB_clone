#!/usr/bin/python3
from models.base_model import BaseModel

class Amenity(BaseModel):
    """Define Amenity Class.

    Attributes:
    name(str): The name of the amenity.
    """
    name = "" # The amenity is set as an empty string.
